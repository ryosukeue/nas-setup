# 友人向け写真NAS 構成案

## 目的

- LANケーブルをつなぐだけでIPアドレスを自動取得する
- 利用者はCLIを使わず、ブラウザだけで初期設定を完了できる
- Windows/macOSから写真フォルダをネットワークドライブとして利用できる
- スマホ写真とPC写真をImmichの同じタイムラインで閲覧できる
- 所有者の許可の範囲で、販売者がTailscale経由の遠隔保守を行える
- 通常の保守権限から写真データを分離する

## 推奨構成

```text
友人のLAN
  ├─ ルーター（DHCPでNASへIPを割り当てる）
  ├─ PC ── SMB ───────────────┐
  ├─ スマホ ── Immichアプリ ──┤
  └─ ブラウザ ─ nas.local ────┤
                                ▼
                         Ubuntu Server NAS
                         ├─ Avahi（mDNS）
                         ├─ 初期設定Web UI
                         ├─ Samba（PC写真共有）
                         ├─ Immich（Docker）
                         ├─ Tailscale
                         └─ Btrfs/バックアップ
                                ▲
                                │ Tailscale SSH（保守専用・制限付き）
                           販売者のPC
```

### LAN自動接続

- NAS自身をDHCPサーバーにはせず、家庭のルーターからDHCPでアドレスを受け取る。
- Avahiで `nas.local` とWeb/SMBサービスをmDNS/DNS-SD広告する。
- 初回案内カードに `http://nas.local` とQRコードを印刷する。
- `.local` が使えないPC向けに、ルーター上のIPアドレスでも開けるフォールバックを用意する。
- 完全に任意のドメイン名を家庭LAN全体で使うにはルーター側DNS設定が必要なため、標準は `nas.local` とする。初期設定でホスト名は変更可能にする。

### 初期設定Web UI

リバースプロキシの手前に、小さな専用セットアップサービスを置く。未設定時だけ `http://nas.local` に表示し、完了後は通常のホーム画面へ切り替える。

初回ウィザードの流れ:

1. NAS名、所有者名、タイムゾーンを設定
2. 保存領域とディスク状態を確認
3. PC共有用アカウントとパスワードを作成
4. Immichを起動し、公式の最初の管理者登録画面へ案内
5. Tailscaleの認証URLを表示し、友人自身のアカウントでNASを登録
6. Windows/macOS/iPhone/Androidごとの接続方法を表示
7. 設定完了後、ウィザードをロックして再表示には所有者認証を要求

ImmichやTailscaleの非公開APIへ強く依存せず、公式の登録画面をウィザード内の手順としてつなぐ。これにより将来のアップデートで壊れにくくする。

### PCネットワークマウントの初期設定

初期設定Web UIで、NAS側の共有フォルダ作成、Sambaユーザー作成、パスワード設定、権限設定、サービス広告までは自動化できる。

一方、一般的なWebブラウザにはPCのネットワークドライブ設定を書き換える権限がない。このため、ブラウザだけでWindows/macOSへ無確認でマウントすることはできない。次の方法で利用者の操作を最小化する。

- AvahiとWS-Discoveryで、Windowsの「ネットワーク」とmacOSのFinderへNASを自動表示する。
- セットアップ完了画面にOS自動判定した「写真フォルダを接続」ボタンを表示する。
- macOSでは `smb://nas.local/Photos` を開く案内と、キーチェーン保存を案内する。
- Windowsでは共有パスのコピーに加え、ネットワークドライブを登録する小さな接続ツールをダウンロードできるようにする。
- 接続ツールは実行前に内容と変更点を表示し、利用者の確認後にだけマウントする。
- 将来的に完全なワンクリック化が必要なら、署名済みWindows/macOS用ヘルパーアプリを用意する。

つまり、NAS側設定はウィザードへ完全に埋め込める。PC側はOSの安全制限により最後のクリックと認証情報保存だけ利用者に行ってもらう。

### 写真の保存と統合表示

保存領域を次のように分離する。

```text
/srv/photos/pc/          PCからSMBで保存する写真
/srv/immich/library/     スマホからImmichへアップロードした原本
/srv/immich/postgres/    Immichデータベース
/srv/backups/            設定・DBバックアップ（保存先は別途検討）
```

- Sambaの `Photos` 共有を `/srv/photos/pc` に割り当て、友人のアカウントだけ読み書き可能にする。ゲストアクセスは禁止する。
- Immichコンテナには `/srv/photos/pc` を読み取り専用の外部ライブラリとして渡す。
- 外部ライブラリの所有者を友人のImmichユーザーにし、定期スキャンする。
- スマホのアップロードライブラリとPCの外部ライブラリは、Immich上では同じ利用者のタイムラインに表示される。
- PC側のフォルダ操作を原本管理の正とし、ImmichからPC写真を削除・変更させない。

Immichのアカウント作成前から、ディレクトリ作成、アクセス権、Dockerの読み取り専用マウントまでは設定しておく。外部ライブラリはImmichユーザーを所有者として指定する必要があるため、アカウントが存在する前には完成できない。

初期設定サービスは次の順序で処理し、利用者からは最初のログイン時点ですでに設定済みに見えるようにする。

1. `/srv/photos/pc` を作成し、SambaとImmichの読み取り権限を設定
2. Immichコンテナへ `/mnt/external/pc:ro` としてマウント
3. 友人がImmichの最初の管理者アカウントを作成
4. 初期設定サービスが作成完了を検出
5. 公式APIを使って、そのユーザー所有の外部ライブラリとインポートパスを作成
6. 初回スキャンを開始してからImmichホームへ移動

ImmichのAPI変更に備え、対応バージョンを固定し、アップデート前の互換性テストを行う。APIで安全に作成できないバージョンでは、最後の1画面だけImmich公式管理画面へ案内するフォールバックを持たせる。

注意点: Immichの重複判定はライブラリをまたいで完全には行われないため、同じ写真をスマホアップロードとPC共有の両方へ入れると二重表示になる可能性がある。「スマホ写真はImmichアプリ、既存のPC写真はPhotos共有」という運用ルールを初期画面に明記する。

### 外出先からの利用と遠隔保守

- NASは友人が所有するTailnetへ参加させる。現在の販売者側Tailnet登録は引き渡し前に解除する。
- 友人のスマホとPCにもTailscaleを入れ、外出先からImmichへ接続できるようにする。
- NASに `tag:nas` を付け、アクセス制御で利用者と保守者を分離する。
- 友人はImmichと必要なNASサービスへアクセス可能にする。
- 販売者はTailscale SSHの保守専用ユーザー `support` にだけ接続可能にし、ImmichとSMBのポートは許可しない。
- SSHは再認証を要求するcheckモードを使い、アクセスルールにテストも追加する。

## 写真を販売者から見えなくする方法

### 推奨する現実的な方式

- `support` ユーザーを写真所有グループに入れず、写真ディレクトリの権限を与えない。
- `support` に通常の `sudo` は与えない。
- 再起動、状態確認、更新、サービス再起動など、引数を固定した保守コマンドだけを実行できるラッパーを用意する。
- 保守画面やログには写真のファイル名・パスをできるだけ出さない。
- 大きな修理に管理者権限が必要な場合は、友人がWeb画面から時間制限付きの「保守モード」を明示的に有効化する。
- すべての保守操作をjournalへ記録し、所有者がWeb画面で確認できるようにする。

### 限界

Linuxの完全なroot権限を持つ人から、稼働中のサーバー上にある平文写真を暗号学的に隠すことはできない。Immich自身がサムネイル生成や検索のため原本を読む必要があるため、ディスク暗号化だけでも起動・解除後のrootアクセスは防げない。

したがって「販売者に写真フォルダの通常アクセス権を与えない」は実現できるが、「悪意ある完全管理者でも絶対に読めない」と「販売者が何でも遠隔修理できる」は同時には実現できない。標準状態を制限付き保守にし、必要時だけ友人の承認で一時昇格する構成を推奨する。

## 保守権限リクエストと所有者承認

NASホーム画面に「保守アクセス」ページを設ける。ここで所有者は現在の権限、過去の申請、操作履歴、有効期限を確認できる。

推奨フロー:

1. 販売者がTailscale SSHで `nas-support request` を実行
2. NASが用途、申請者、期限、ランダムな申請IDを記録
3. 友人のAndroidへ「保守権限の申請」と通知
4. 友人が通知からNASの承認ページを開く
5. NAS所有者のパスキーまたはパスワードで本人確認
6. 「許可」「拒否」と、許可時間（標準1時間）を選択
7. 許可された範囲の保守機能だけ有効化
8. 期限到来時に権限を失効し、販売者の保守セッションを終了
9. 開始、実行操作、終了を監査ログに保存し、双方へ通知

通知のボタンを押しただけでroot権限を付けず、必ずNAS側の期限付き承認ページで本人確認する。通知サービスのアカウントやリンクが第三者へ渡った場合の誤承認を防ぐためである。

権限は次の3段階に分ける。

- 通常保守: 状態確認、更新確認、サービス再起動などの固定コマンド。写真アクセスなし。
- 拡張保守: 所有者が1時間だけ許可。設定変更やパッケージ更新など、範囲を広げた保守操作。
- 緊急管理: 完全なroot作業。写真への技術的アクセスを否定できなくなることを画面に明示し、別の強い確認を要求。

単に `support` をsudoグループへ追加する方式では、期限前にrootシェルを残せてしまう。通常・拡張保守はroot権限の小さな仲介サービスに許可済み操作だけを依頼する方式とし、任意のrootシェルを渡さない。緊急管理を使った場合は、期限時にセッションと関連プロセスを終了するが、完全なrootを一度渡した以上は技術的な絶対保証はできない。

### Android通知

通知部分は交換可能な仕組みにし、初期設定画面で選択できるようにする。

- Telegram: 既に利用している場合に導入が簡単。Botのボタンから承認ページを開ける。
- ntfy: オープンソースのAndroidアプリで、通知にアクションボタンを付けられる。
- メール: 予備経路。即時性は低い。

初期版はTelegramとntfyの両方に対応できる設計にし、実装順は友人が普段使っている方を優先する。Botトークン、通知トークン、申請URLは画面やGitHubへ記録せず、rootだけが読める秘密領域へ保存する。

## RAID・HDD監視

HDD故障の早期発見は可能で、次の複数の検査を組み合わせる。SMARTだけ、またはRAID状態だけでは検出範囲が不足するためである。

- `smartd` で各HDDのSMART異常、代替セクタ、保留セクタ、温度、読み取りエラーを常時監視
- SMART短時間テストを週1回、長時間テストを月1回実行
- `btrfs device stats --check` でデバイスI/Oエラーを毎日確認
- Btrfsのデバイス欠落、データ/メタデータのRAIDプロファイル、容量不足を毎日確認
- Btrfs scrubを月1回実行し、チェックサムエラーと修復不能エラーを確認
- Docker、Immich、PostgreSQL、Tailscale、Sambaの稼働状態も軽く確認

異常時は友人と販売者へ、故障したディスクのスロット、型番、シリアル末尾、現在の冗長性、推奨行動を通知する。同じ異常を連続送信せず、初回、24時間後の未解決通知、復旧時に送る。

NAS全体の電源断や通信断ではNAS自身から通知できない。その検出も必要なら、NASから外部サービスへ定期的に生存通知を送り、途絶時に外部からAndroidへ通知するハートビート監視を追加する。

故障検出後に自動でディスク交換やRAID再構築は開始しない。誤検出や別ディスク指定の事故を避け、所有者への通知と保守申請までを自動化し、交換処理は確認後に行う。

## セキュリティと運用

- UFWでLAN/Tailscale以外からの接続を拒否する。
- SMBはLANだけに公開し、Tailscale越しに必要なら友人の端末だけ許可する。
- Immich、セットアップUI、OSの更新方法を固定し、更新前にDBと設定をバックアップする。
- ディスク故障、容量不足、バックアップ失敗をホーム画面に日本語で表示する。
- RAID/Btrfsの冗長化はバックアップではない。USB外付けディスクまたは別拠点への暗号化バックアップを追加する。
- OSディスク暗号化を使う場合、停電後の無人復帰と秘密保持のどちらを優先するか所有者に選んでもらう。
- セットアップの再実行、工場出荷状態への復元、所有権移転の手順を用意する。

## 実装段階

### Phase 1: 基盤

- 現在の構成をバックアップ
- ホスト名、DHCP、Avahi、固定サービス名を整備
- Sambaとディレクトリ権限を構築
- Immich外部ライブラリでPC写真を表示できることを検証

### Phase 2: 初期設定画面

- 一回限りのセットアップWeb UIを実装
- Immich/Tailscaleの公式初期設定を一つの流れに統合
- Windows/macOS/スマホ向け接続案内とQRコードを生成
- PC共有の自動設定とOS別接続ヘルパーを実装
- Immich管理者作成直後の外部ライブラリ自動関連付けを実装
- 再実行・失敗復旧を検証

### Phase 3: 遠隔保守と引き渡し

- 友人所有Tailnetのアクセス制御を作成
- 制限付き `support` ユーザーと保守コマンドを実装
- 通知、所有者承認、一時保守モード、強制失効、監査ログを実装
- 現在の認証情報・Tailnet・テストデータを消去して引き渡しテスト

### Phase 4: 安全運用

- 自動バックアップ、復元テスト、容量・ディスク監視
- SMART、Btrfs device stats、月次scrub、Android通知、外部ハートビートを実装
- 更新失敗時のロールバック手順
- 利用者向けの短い紙マニュアルを作成

## 現時点の推奨判断

このNASは「Ubuntuを隠して専用アプライアンスのように見せる」構成にする。利用者に見せる入口は `nas.local` のホーム画面だけにし、そこから初期設定、Immich、接続案内、状態確認へ移動できるようにする。

写真の扱いは、PC写真を読み取り専用のImmich外部ライブラリ、スマホ写真をImmichアップロードライブラリに分ける。遠隔保守は友人所有のTailscaleと制限付きSSHを標準にし、完全管理権限は友人の明示承認がある時間だけ有効にする。
