# 友人向け写真NAS 製品設計

更新日: 2026-09-04

## 製品方針

Ubuntuを意識せずに使える小さな写真アプライアンスとして渡す。利用者が行うのは、LAN接続、`http://nas.local` での初期登録、Windows接続ツールの一度の実行、スマホアプリへの登録だけとする。

販売者による継続サポートは製品要件から外した。遠隔保守アカウント、権限リクエスト、所有者承認、一時管理者権限は実装しない。販売者から写真やImmichのデータへ到達できる経路を引き渡し後に残さない。

## 利用者が得る機能

- Windowsの `Photos` ネットワークドライブへPC写真を保存
- Immichアプリへスマホ写真を自動アップロード
- PC写真をImmichの読み取り専用外部ライブラリとして同じ画面に表示
- 所有者自身のTailscale経由で、外出先からImmichへ接続
- RAIDメンバー消失、物理HDD消失、SMART総合異常をntfyで通知

## 初回セットアップ

所有者が入力する項目は名前、メールアドレス、パスワードである。

- 名前: Immichの表示名
- メールアドレス: ImmichのログインID
- パスワード: Immich、NAS管理画面、Windows共有の初期パスワード

セットアップサービスはSambaユーザー、Immich最初の管理者、`PC Photos`外部ライブラリ、ntfyのランダムトピックを順に作る。所有者がImmichへ初めてログインした時点で、PC写真ディレクトリは登録済みで初回スキャンが始まっている。

## Windows共有

Samba/SMB 2・3を使用する。SMBはWindows標準のファイル共有方式で、WS-Discoveryによりエクスプローラーの「ネットワーク」にNASを表示する。

WebブラウザはWindowsのネットワークドライブ設定を変更できないため、完全自動マウントはできない。画面からダウンロードする `.cmd` を所有者が一度実行し、共有パスワードを入力する。ツールは空いているドライブ文字を選び、Windows資格情報と永続マッピングを保存する。通常は再起動後も再接続する。Microsoft署名済みアプリの開発・配布は、この用途には重すぎるため採用しない。

## 写真統合

- `/srv/photos/pc` はSambaから所有者が読み書きする
- Immichは同じ場所を `/mnt/external/pc:ro` として読み取る
- `/srv/immich/library` はスマホからのImmichアップロードを保存する
- PC写真とスマホ写真は同じ所有者のImmich画面へ表示する

PC写真はImmichから削除・変更させず、Windows側を原本管理の正とする。同一写真をPC共有とスマホアップロードの両方へ入れると二重表示になる場合がある。

## 外出先アクセス

NAS、所有者のスマホ、必要なPCを所有者本人のTailnetへ参加させる。NASはTailscale ServeでImmichをTailnet内HTTPSとして公開する。これはインターネット一般公開ではなく、そのTailnetへログインした所有者の端末だけが利用できる。

引き渡し前の構築用Tailnetは初期設定中に解除する。外出先接続確認後、所有者が引き渡し完了を押すと、構築用 `ryo` ログインも停止する。

## ntfyとHDD監視

ntfy.shの推測困難なランダムトピックを生成し、管理画面へスマホ用QRとPC用URLを表示する。所有者はアプリでQRを読むか、PCブラウザでURLを開く。ntfyのアカウント登録やAPIキーは不要である。トピックURLを知る人は通知を購読できるため、URLはパスワード同様に扱い、通知本文に写真名や認証情報を載せない。

起動時と1時間ごとにRAID1の2台在席、`[UU]`、物理デバイスの存在、SMART総合判定を確認する。異常時と復旧時に通知し、未解決なら24時間後に再通知する。故障検出後のディスク交換や再構築は自動実行しない。NAS全体が停止した場合は自分で通知できないため、必要なら将来外部ハートビートを追加する。

## セキュリティと引き渡し

- Sambaのゲストアクセスは禁止する
- PC写真はImmichへ読み取り専用で渡す
- ランタイム秘密情報はroot専用領域へ保存しGitHubへ入れない
- 販売者用アカウントは作らない
- 引き渡し完了時に構築用アカウントを停止する
- Tailscaleは所有者の外出先Immich用途に限定する

引き渡し後に修理が必要な場合は、所有者が現地操作または明示的な別手段を選ぶ。常設の販売者SSH経路はない。root権限を持つ人から稼働中の平文写真を暗号学的に隠すことはできないため、写真へ到達できる管理経路自体を残さない方針である。

## 対象外

- 販売者による継続的な遠隔サポート
- RAIDとは別の自動バックアップ
- NAS電源断の外部監視
- 自動ディスク交換・自動RAID再構築
- 署名済みWindows接続アプリ

RAID1は1台故障時の継続運転のための冗長化であり、バックアップではない。大切な写真には別のUSBディスクまたは別拠点へのバックアップが必要である。
